{
  "name": "angular-microfrontend-architecture",
  "version": "1.0.0",
  "description": "Angular 20 Microfrontend Architecture with Module Federation",
  "scripts": {
    "build": "npm run build:shell && npm run build:customers-mf",
    "build:shell": "cd shell && npm install && npm run build",
    "build:customers-mf": "cd customers-mf && npm install && npm run build",
    "start": "npm run start:shell",
    "start:shell": "cd shell && npm run start",
    "start:customers-mf": "cd customers-mf && npm run start",
    "test": "npm run test:customers-mf",
    "test:customers-mf": "cd customers-mf && npm run test",
    "test:shell": "cd shell && npm run test",
    "install:all": "npm install && cd shell && npm install && cd ../customers-mf && npm install",
    "dev": "concurrently \"npm run start:shell\" \"npm run start:customers-mf\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "shell",
    "customers-mf"
  ],
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  }
}
