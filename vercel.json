{
  "version": 2,
  "name": "angular-microfrontend",
  "builds": [
    {
      "src": "shell/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    },
    {
      "src": "customers-mf/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    },
    {
      "src": "api/customers.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/customers/(.*)",
      "dest": "/api/customers.js"
    },
    {
      "src": "/customers/(.*)",
      "dest": "/customers-mf/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/shell/$1"
    }
  ],
  "env": {
    "ENABLE_EXPERIMENTAL_COREPACK": "1"
  },
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "echo 'Build handled by individual builds'"
}